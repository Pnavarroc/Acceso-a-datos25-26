info:
  name: CreateProduct
  type: http
  seq: 3

http:
  method: POST
  url: http://127.0.0.1:8080/api/productos
  body:
    type: json
    data: |-
      {
        "nombre": "POllas en Vinagre",
        "precio": 300,
        "categoria": {
          "id":1,
          "nombre": "Lacteos" 
        }
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
